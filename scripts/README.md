Bash Scripts
Consider backend-bootstrap.sh only if using linux OS. This doesnt work on Windows OS

Python Scripts
bootstrap.py        → orchestrates
backend_bootstrap   → backend terraform only
bootstrap_infra     → infra/ansible only
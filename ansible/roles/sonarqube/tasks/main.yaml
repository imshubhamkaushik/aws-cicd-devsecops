---
# SonarQube's embedded Elasticsearch requires vm.max_map_count >= 524288.
- name: Set vm.max_map_count for Elasticsearch (required by SonarQube)
  sysctl:
    name: vm.max_map_count
    value: "524288"
    state: present
    reload: yes
    sysctl_file: /etc/sysctl.d/99-sonarqube.conf

# Create SonarQube data directories
- name: Create Sonar directories
  file:
    path: "{{ item }}"
    state: directory
    mode: '0755'
    owner: "1000"
    group: "1000"
  loop:
    - /opt/sonarqube/data
    - /opt/sonarqube/logs
    - /opt/sonarqube/extensions

# Run SonarQube container
- name: Run SonarQube container
  # FQCN is required when the collection is installed via requirements.yml. The collection itself must be installed:
  #   ansible-galaxy collection install -r requirements.yml
  community.docker.docker_container:
    name: sonarqube
    image: sonarqube:26.3.0.120487-community
    state: started
    restart_policy: unless-stopped
    ports:
      - "9000:9000"
    env:
      SONAR_ES_BOOTSTRAP_CHECKS_DISABLE: "true"
      SONAR_WEB_JAVAOPTS: "-Xms256m -Xmx512m"
      SONAR_CE_JAVAOPTS: "-Xms256m -Xmx512m"
      SONAR_SEARCH_JAVAOPTS: "-Xms256m -Xmx512m"
    volumes:
      - /opt/sonarqube/data:/opt/sonarqube/data
      - /opt/sonarqube/logs:/opt/sonarqube/logs
      - /opt/sonarqube/extensions:/opt/sonarqube/extensions

# Wait for SonarQube to be up and running before finishing the playbook
- name: Wait for SonarQube to be healthy
  uri:
    url: "http://localhost:9000/api/system/status"
    method: GET
    status_code: 200
    return_content: yes
  register: sq_status
  until: sq_status.json is defined and sq_status.json.status == "UP"
  retries: 30
  delay: 10

# Change default admin password
- name: Change SonarQube admin password
  uri:
    url: "http://localhost:9000/api/users/change_password"
    method: POST
    user: admin
    password: {{ item }}
    force_basic_auth: yes
    body_format: form-urlencoded
    body:
      login: admin
      previousPassword: {{ item }}
      password: "{{ vault_sonar_admin_password }}"
  loop:
    - admin
    - "{{ vault_sonar_admin_password }}"
  register: change_password
  failed_when: false

# Generate Sonar token (idempotent - only generates if token doesn't already exist)
- name: Generate SonarQube token
  uri:
    url: "http://localhost:9000/api/user_tokens/generate"
    method: POST
    user: admin
    password: "{{ vault_sonar_admin_password }}"
    force_basic_auth: yes
    body_format: form-urlencoded
    body:
      name: "jenkins-token"
  register: sonar_token_response
  failed_when: >-
    sonar_token_response.status != 200 and not (
      sonar_token_response.json is defined and
      sonar_token_response.json.errors is defined and
      'Name already exists' in (sonar_token_response.json.errors | map(attribute='msg') | join(','))
    )
  when: vault_sonar_token == ""

# Store token as fact for later use (e.g. Jenkins configuration)
- name: Set Sonar token fact
  set_fact:
    sonar_token: "{{ sonar_token_response.json.token }}"
  when: sonar_token is not defined

# Persist Sonar token to Vault
- name: Persist/Save Sonar token to vault file (first run only)
  delegate_to: localhost
  run_once: true
  when: sonar_token_response is defined
  vars:
    vault_file: "{{ playbook_dir }}/group_vars/all/vault.yaml"
    vault_pass: "{{ lookup('env','HOME') }}/.vault_pass"
  block:

    - name: Decrypt vault file
      command: >
        ansible-vault decrypt {{ vault_file }}
        --vault-password-file {{ vault_pass }}

    - name: Append token to vault file if not already present
      shell: |
        grep -q "^sonar_token:" {{ vault_file }} || \
        echo "sonar_token: {{ sonar_token }}" >> {{ vault_file }}
      changed_when: "'sonar_token:' not in lookup('file', vault_file)"

  always:
    - name: Re-encrypt vault file (always)
      command: >
        ansible-vault encrypt {{ vault_file }}
        --vault-password-file {{ vault_pass }}
      ignore_errors: true